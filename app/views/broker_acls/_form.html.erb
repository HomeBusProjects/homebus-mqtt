<%= form_with(model: broker_acl) do |form| %>
  <% if broker_acl.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(broker_acl.errors.count, "error") %> prohibited this broker_acl from being saved:</h2>

      <ul>
        <% broker_acl.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :topic, style: "display: block" %>
    <%= form.text_field :topic %>
  </div>

  <div>
    <%= form.label :permissions, style: "display: block" %>
    <%= form.number_field :permissions %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
